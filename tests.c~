#include <stdbool.h>

#include "lc3sim.h"
#include "instructions.h"

int main()
{
  bool b = false;
  lc3machine_t m;
  m.pc = 0;

  /*
   * ADDI
   */
  m.memory[1] = INSTRUCTION_ADDI;
  (m.registers)[0] = 0;
  LI_SET_DR(m.memory[1], 0);
  
  lc3k_step(&m);

  b = b || m.
  
  return b
}
